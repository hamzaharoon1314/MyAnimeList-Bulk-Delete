# MyAnimeList-Bulk-Delete
Adds Inject Script and Delete All buttons to MyAnimeList anime lists. Scans the list, collects anime IDs, deletes them automatically, and shows a live progress bar while entries are removed.
